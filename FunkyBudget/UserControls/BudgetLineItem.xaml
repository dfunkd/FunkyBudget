<UserControl x:Class="FunkyBudget.UserControls.BudgetLineItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FunkyBudget.UserControls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FunkyBudget.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="75" />
            <ColumnDefinition Width="75" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" FontWeight="Bold" Foreground="{DynamicResource Lightest}" HorizontalAlignment="Right" Margin="5, 0" Text="{Binding PayTo}" />
        <TextBlock Grid.Column="1" FontWeight="Bold" Foreground="{DynamicResource RuanGreen}" HorizontalAlignment="Right" Margin="5, 0" Text="{Binding AmountDue}" />
        <CheckBox Grid.Column="2" Click="OnPaidChanged" Content="Is Paid" FontWeight="Bold" Foreground="{DynamicResource Lightest}" HorizontalAlignment="Center"
                  IsChecked="{Binding IsPaid}" Margin="5, 0" />
    </Grid>
</UserControl>
