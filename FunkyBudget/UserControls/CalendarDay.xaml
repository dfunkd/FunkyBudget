<UserControl x:Class="FunkyBudget.UserControls.CalendarDay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:FunkyBudget.UserControls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FunkyBudget.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="125"
             d:DesignWidth="200"
             Loaded="OnLoad">

    <UserControl.DataContext>
        <vm:CalendarDayViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Foreground="{DynamicResource Lightest}" Margin="5" Text="{Binding DueDate.Day}" />
        <DockPanel x:Name="dpBills" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" />
    </Grid>
</UserControl>
